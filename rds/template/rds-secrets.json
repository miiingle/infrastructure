{
  "username" : "${username}",
  "password" : "${password}",
  "engine" : "${engine}",
  "host" : "${host}",
  "dbname" : "${dbname}",
  "port" : "${port}",
  "dbInstanceIdentifier": "${dbInstanceIdentifier}"
}