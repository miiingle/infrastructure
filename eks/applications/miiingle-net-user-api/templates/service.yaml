apiVersion: v1
kind: Service
metadata:
  name: {{ include "miiingle-net-user-api.fullname" . }}
  labels:
    {{- include "miiingle-net-user-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miiingle-net-user-api.selectorLabels" . | nindent 4 }}
